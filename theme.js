import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4c9ab9',
    },
    secondary: {
      main: '#8acce6',
    },
    accent: {
      main: '#4dc3f0',
    },
    foreground: {
      main: '#f7f9fa',
    },
    background: {
      main: '#191a1c',
    },
  },
});

export default theme;
